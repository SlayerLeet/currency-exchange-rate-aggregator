# WEB Currency Parser and Database Storage

Веб-приложение с REST API для парсинга курсов валют и криптовалют, сохранения данных в базу и визуализации динамики курсов в виде графиков.

Проект предназначен для автоматического сбора данных о курсах, их хранения и последующего анализа через веб-интерфейс.

---

## 🚀 Возможности

* Парсинг курсов валют и криптовалют из внешних источников
* Асинхронные HTTP-запросы
* Сохранение данных в базу данных
* REST API для получения курсов
* Веб-интерфейс с графиками изменения курсов
* Обновление данных с заданным интервалом (например, каждые 10 минут)

---

## 🧱 Технологии

**Backend:**

* Python 3.10+
* FastAPI
* httpx (асинхронные запросы)
* asyncio

**Frontend:**

* HTML (Jinja2 templates)
* JS (графики на клиенте)

**Парсинг:**

* Регулярные выражения
* HTTP-запросы к внешним источникам

---

## 📁 Структура проекта

```text
.
├── main.py                # Точка входа FastAPI
├── parser.py              # Логика парсинга курсов
├── custom_currensis.py   # Работа с кастомными валютными парами
├── templates/
│   └── index.html        # Главная страница
├── README.md
```

---

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/SlayerLeet/WEB-Currency-parser-and-saving-to-database.git
cd WEB-Currency-parser-and-saving-to-database
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # Linux / macOS
venv\Scripts\activate     # Windows
```

### 3. Установка зависимостей

```bash
pip install fastapi uvicorn httpx
```

(При необходимости добавь `jinja2`)

### 4. Запуск сервера

```bash
uvicorn main:app --reload
```

После запуска:

* API будет доступно по адресу:
  `http://127.0.0.1:8000`
* Веб-интерфейс:
  `http://127.0.0.1:8000/`

---

## 📡 API

### Получение главной страницы

```http
GET /
```

Возвращает HTML-страницу с курсами.

---

## 🔄 Парсинг данных

Логика парсинга реализована в `parser.py`:

* Используется `httpx` для асинхронных запросов
* Данные извлекаются из HTML с помощью regex
* Результаты приводятся к структурированному виду

Пример запуска парсера вручную:

```bash
python parser.py
```

---

## 🧪 Тестирование

Тесты пока не реализованы.
Планируется добавить unit-тесты для:

* парсинга курсов
* обработки ошибок сетевых запросов
* валидации данных

---

## ⚠️ Ограничения

* Нет обработки ошибок сети (ретраи, таймауты)
* Нет нормализованной схемы БД (на текущий момент)
* Нет авторизации в API
* Источник данных может менять HTML-структуру

---


## 👤 Автор

SlayerLeet
GitHub: [https://github.com/SlayerLeet](https://github.com/SlayerLeet)

